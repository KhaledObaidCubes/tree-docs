import { Options as TransformOptions } from 'sucrase';
import { EvaluableComponent } from 'vue-inbrowser-compiler-utils';
interface EvaluableComponentWithSource extends EvaluableComponent {
    raw: {
        script: string;
        template?: string;
    };
}
export declare function compileVue3Template(template: string, component: any): () => any;
/**
 * Reads the code in string and separates the javascript part and the html part
 * then sets the nameVarComponent variable with the value of the component parameters
 * @param code
 * @param config sucrase config to be used when transforming
 *
 */
export default function compileVueCodeForEvalFunction(code: string, config?: Omit<TransformOptions, 'transforms'> & {
    objectAssign?: string;
}, scopeId?: string): EvaluableComponentWithSource;
export {};
